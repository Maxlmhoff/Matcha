<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Matcha</title>
        
         <%- css.style %>
    </head>
 
    <body background=../background.png>
        <% include header.ejs %>
    <div class="log" style="height: 290px;">
        <h2> Login </h2>
        <form action="login" method="post">
            <input type="text" placeholder="Login" name="login">
            <input type="password" placeholder="Passwd" name="pass">
            <input type="submit" value="login">
        </form>
        <% if (typeof error !== 'undefined' && error && error !== 'none') { %> <div id=error><strong>Error: <%- error %></strong></div> <% } %>
        <% if (typeof success !== 'undefined' && success) { %> <div id=success><strong>Success: <%- success %></strong></div> <% } %>
    </div>
    </body>
</html>